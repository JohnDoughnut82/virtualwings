<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VirtualWing"
        tools:targetApi="31">
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/AppTheme.DCS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.home.HomeActivity" android:theme="@style/AppTheme.DCS"/>
        <activity android:name=".ui.userProfile.ProfileActivity" android:theme="@style/AppTheme.DCS"/>
        <activity android:name=".ui.flightLog.FlightLogActivity" android:theme="@style/AppTheme.DCS"/>
        <activity android:name=".ui.signup.SignUpActivity" android:theme="@style/AppTheme.DCS"/>
        <activity android:name=".ui.flightLog.ViewLogsActivity" android:theme="@style/AppTheme.DCS"/>
        <activity android:name=".ui.squadron.SquadronLandingActivity" android:theme="@style/AppTheme.DCS"/>
        <activity android:name=".ui.squadron.SquadronInfoActivity" android:theme="@style/AppTheme.DCS"/>
        <activity android:name=".ui.squadron.SquadronJoinActivity" android:theme="@style/AppTheme.DCS"/>
        <activity android:name=".ui.squadron.SquadronCreateActivity" android:theme="@style/AppTheme.DCS"/>
    </application>

</manifest>